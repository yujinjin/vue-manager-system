/**
 * 作者：yujinjin9@126.com
 * 时间：2022-02-24
 * @param module 页面模块
 * @param name 名称
 * 描述：分页查询页面组合式API
 */
// import { AxiosPromise } from "axios";
// import { reactive, ref } from "vue";

export default function () {
    // // 查询表单
    // const searchForm: Record<string, any> = reactive(Object.assign({}, searchParameters));
    // // 当前列表查询状态
    // const searchLoading = ref(false);
    // // 当前查询的列表数据
    // const dataList = ref([]);
    // // 分页信息
    // const pagination = reactive({
    //     total: 0, // 总记录数
    //     pageSize: 10, // 每页记录数
    //     currentPage: 1, // 当前页记录数
    //     layout: "total, sizes, prev, pager, next, jumper",
    //     pageSizes: [10, 20, 50, 100]
    // });
    // // 分页查询
    // const queryDataList = function(isInit: boolean = true, API: AxiosPromise<>, isPagination: boolean = true) {
    //     if (searchLoading.value) {
    //         return;
    //     }
    //     if (isInit && isPagination) {
    //         searchForm.
    //     }
    // }
    // return { searchForm, searchLoading, dataList, pagination, queryDataList };
}
